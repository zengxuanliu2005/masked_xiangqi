# Changelog

## [Unreleased]

## [1.2.0] - 2026-09-10

### Added

- 人机对战新增简单、中等、困难三档难度；档位贯通建局 API、公开局面、独立 Runner、对手标签与同 Seed 重开。
- 三档 Ollama 策略文本支持 `.local/strategies/` 本地覆盖；困难档用公开称谓、子力价值和揭面状态标注候选着法。

### Changed

- `npm run dev` 改为单进程：Express 在 3001 同时提供 API 与 Vite 中间件，设置中的开关可在同一进程内切换本机/LAN 监听；`--lan` 仍可指定启动时模式。
- 各难度分别控制输出预算、thinking 与首次/纠错超时，单步两次请求的总预算均不超过 60 秒。
- 移除不再需要的 `concurrently` 开发依赖。

### Security

- 困难档候选标注只从 `PublicGameState` 推导，不读取或泄漏未翻棋子的真实身份。
- Vite 开发中间件拒绝提供 `.local/**`、`.env*` 与 `.git/**`；AI 策略覆盖拒绝软链接、命名管道等非普通文件及超过 8 KiB 的文件。
- Vite 的独立 HMR WebSocket 始终绑定回环地址，关闭 LAN 后不会遗留可从同网段访问的开发 listener。
- 同步升级 Vitest 与 V8 coverage 至 4.1.11，并将间接依赖 `qs` 刷新至 6.16.0，修复当前依赖审计中的路径读取与拒绝服务公告。

### Fixed

- 人机设置中的难度区与胜负规则区使用独立网格行，不再重叠。
- Ollama 测试使用隔离的策略目录，不受开发者本地覆盖文件影响。
- Agent Runner 按实际请求报告 GPT-OSS 的 `low` / `medium` 思考级别。

### Documentation

- README 改为图文特色介绍，展示三档 AI 难度、揭面反转与联机玩法；安装与使用说明迁入使用指南，既有实现细节归入本文末尾。
- 仓库仅保留 README 引用的成品图片；宣传贴文、原始截图、排版源文件、配套图片和打包文件改为本地保留，不再跟踪。
- 本地 Agent 文档同步钩子不再跟踪；发布审计检查全部跟踪文件，防止本地素材与维护文件重新进入仓库。

## [1.1.0] - 2026-09-01

### Added

- 局域网双人对战：`POST /rooms` 建房、6 位房间码加入、每方独立视角与操作门控。
- 座位令牌鉴权（`Authorization: Bearer`），写操作按座位校验，读取保持开放。
- 悔棋协商：只有刚落子的一方可发起，需对手在自己设备上同意；局面前进、超时或终局自动失效。
- 房主可「重新邀请」作废客人座位并签发新房间码，界面显示对手在线状态。
- 可选的局域网监听（`--lan` / `LAN=1` / 首页开关），以及包含全部候选私网地址的邀请链接。
- `GET/POST /api/v1/network` 提供 `pending`、`targetMode`、`listening` 真实过渡状态。

### Security

- 本机身份只取规范化的 socket 对端地址；不信任 `Host`、`Forwarded`、`X-Forwarded-For` 或 `X-Real-IP`。
- loopback Host 只接受 loopback 对端；LAN Host 必须是服务器当前真实网卡的私网 IP 字面量，拒绝域名与其他私网地址。
- LAN 请求还必须来自 loopback 或私网 socket 对端；LAN Origin 必须与请求 Host 的 IP 和端口同时一致，拒绝公网转发与同 IP 异端口页面。
- `POST /games`、`POST /rooms`、AI、`agent-session*` 与网络开关无条件仅限真实 loopback；非 LAN 对局的落子、认输和悔棋也仅限 loopback，远程调用统一返回 `403 LOOPBACK_ONLY`。
- 公布的局域网地址复用同一套放行判定，不会给出必然被拒的加入链接（CGNAT/Tailscale 有意排除）。
- 猜错房间码才计入限流；已满或已作废的房间码不会牵连同网段其他人加入。

### Fixed

- `undoTargetIndex` 对非人机对局统一撤回单步，避免新增对局类型落入人机分支。
- 监听关闭、绑定与失败回滚进入单一 FIFO，消除并发切换遗留 listener 或错报模式的竞态。
- 客户端每 150 ms 查询切换状态（最多 5 秒），短暂断连不再被误报为完成，也不再乐观显示 LAN 已开启；超时后每秒后台对账，另以 5 秒周期/窗口聚焦刷新网卡地址。
- `GameStore.has()` 在不刷新访问时间的前提下执行 TTL 清理，过期 LAN 对局会及时释放容量。
- 每次悔棋请求使用独立 128-bit 随机 ID；resolve 同时匹配 ID 与 revision，旧响应不能处理新请求。
- 新签发的座位立即保存在 React state/ref；localStorage 被禁用时仍可继续持令牌对弈。
- 带座位令牌的读取会投影明确的 `viewer` 凭证状态；替补入座后，旧设备仍能稳定识别自己已被移出。
- 重新邀请同时匹配 `expectedRevision` 与当前房间码；延迟重试不能旋转新码或撤销刚加入的替补。
- 房间存活清理改为一次批量扫描，避免容量上限附近的轮询形成平方级 TTL 检查。
- 网络切换超过 5 秒后继续后台对账；邀请链接会直接打开已预填的加入框。
- 活跃 LAN 局离开前先认输结束房间；结束失败时保留一次性座位与恢复入口，避免房主把自己锁在仍存活的房间之外。
- LAN 落子在房间/回合门控前检查 revision，并发重复写稳定返回 `409 STALE_REVISION`。
- 粘贴带连字符或空格的房间码会先归一化再截到 6 位，不再被浏览器按原始长度提前截断。
- 已撤销座位不能再发起或回应悔棋，相关操作与提示会立即禁用。
- 撤销凭据不再保留旧颜色视角或把替补一方标成「你」；远程访客只看到可用的加入入口。
- 未加载的恢复席位经过模式/教学页导航仍会保留；开始另一局前先确认并结束旧房间，瞬时恢复失败不会删除一次性凭据，只有明确 `GAME_NOT_FOUND` 才清理。
- 换局前协调其他标签页后来签发的座位；每次签发使用单调代次与独立存储槽，旧版标签页的新凭据会提升为下一代，清理以保留逻辑代次的凭据墓碑标记并保留共享指针，较新墓碑会屏蔽所有更旧槽；已持久化凭据的墓碑写入失败会中止换局，纯内存座位仍可正常结束。
- 关机关闭把监听控制器置为永久终态，响应完成后才到达的模式切换不能重新打开端口。
- 替补座位为空时不再启用必然返回 `LAN_WAITING_FOR_OPPONENT` 的悔棋按钮；终局也不再残留等待/撤销遮罩或错误的等待状态。
- 过期旧房间仅删除仍由自己持有的房间码索引，码复用后不会误伤新邀请；LAN 终局结算也不再显示重复的「查看最终棋盘」操作。

## [1.0.0] - 2026-08-31

### Added

- 本机双人/独立 Ollama Runner 对战、两种规则模式、悔棋、认输和终局结算。
- revision 化 REST API、严格请求错误、Agent 会话与跨平台终端启动器。
- 键盘棋盘、焦点管理、移动端恢复、刷新恢复和三浏览器 E2E。
- CI、CodeQL、Dependabot、安全/压力/覆盖率/生产烟测门禁。

### Security

- 活动局 Seed 对所有公开投影、Prompt、状态和日志保密，终局后公开。
- AI single-flight、Runner 决策取消、会话路径约束、日志轮转和资源容量/TTL。
- embedding 模型禁选、模型输出 256 KiB 限制和终端控制字符清洗。

### License

- 以 PolyForm Noncommercial 1.0.0（SPDX `PolyForm-Noncommercial-1.0.0`）发布。

## 既有实现说明（由 README 迁入）

以下记录文档整理时的实现现状，补充上方版本日志；不是本次新增功能，也不改变历史版本归属。安装与日常操作见 [使用指南](docs/GUIDE.md)。

### 暗子与活动局信息

未翻暗子的真实身份只存在于服务端内存中。活动局的 Seed 对浏览器、公开 API、Prompt、Runner 状态和 JSONL 日志均为 `null`，终局后公开。主动指定 Seed 的创建者天然知道该值，隔离保证 API 不额外泄漏服务器生成或保存的活动 Seed。

暗子首次按位置身份行动，随后按揭晓的真实棋种和颜色行动；被吃暗子公开身份。悔棋始终增加 `revision`；人机局回到人类上一轮落子前，同屏与局域网局撤回最近一步。

### 开发服务与局域网生命周期

开发模式使用单一 Express 进程在 3001 提供 API 与 Vite 中间件，保留 HMR。生产模式提供构建产物，启动不依赖 POSIX 环境变量语法。

也可以在运行中从界面开关：关闭、绑定和失败回滚按 FIFO 串行执行。界面每 150 ms 查询一次真实状态（前台最多等待 5 秒），只在新 listener 确认可用后显示切换完成；重绑瞬间的短暂断连会自动重试，前台超时后改为每秒后台对账直至得到终态。页面还会每 5 秒及窗口重新聚焦时刷新真实模式与网卡地址，DHCP 或其他标签页造成的变化无需重载即可反映到邀请卡。进程开始关机后控制器进入终态，晚到的切换请求不会重新打开 listener。

- **悔棋需对方同意**：只有刚走完那一步的一方能发起，对手在自己设备上批准或拒绝。替补尚未入座时不会开放请求；对方先落子、超时或终局都会让请求自动失效。
- **对手掉线**：房主会看到「对手已断线」，可以点「重新邀请」作废旧房间码并生成新的。旧设备会显示「你已被移出对局」。
- **离开或换局**：持有有效座位的设备会先以自己的座位认输并结束房间，成功后才条件清除一次性令牌。普通页面导航不会丢弃尚未恢复的席位；开始另一局会再次确认，瞬时恢复/结束失败会保留凭据以便重试。多个标签页并存时会先协调最新签发记录，旧标签页不能删除或覆盖新座位。
- 开发与生产模式都是单一来源（3001 端口同时提供网页与 API），因此局域网对战在两种模式下都可用，界面上的开关也一样生效。开发模式的 HMR WebSocket 始终只绑定 `127.0.0.1:24678`，确保从 LAN 切回本机后不会遗留对外 listener；其他设备仍可正常使用页面与 API，但源码变化后需要手动刷新。
- mDNS 名称（`xxx.local`）会被 Host 检查拒绝，请使用界面给出的 IP 链接。
- **开发模式下开启局域网会把项目源码目录暴露给同网段设备**，这是 Vite 开发中间件的固有行为。`.local/`（Agent 令牌与日志）、`.env*` 和 `.git/` 已被明确拒绝服务，但 `src/`、`server/` 等源码文件仍可被读取。这与局域网模式本身的信任模型一致：只在可信的家庭网络下使用。要让不完全可信的设备联机，请用生产模式（`npm run build && npm start -- --lan`），它只提供 `dist/`。

### Ollama Agent Runner 与难度

Runner 只读取公开局面和同 revision 的合法着法。Prompt 使用零温度、有限输出预算和严格的 `moveIndex + reason` JSON Schema。无效输出最多纠错一次，之后暂停，绝不随机代走。

#### 难度

开局时可选 **简单 / 中等 / 困难**（缺省中等）。难度记录在对局上并随公开局面传给 Runner，「同 Seed 再来」会沿用同一档。每一档改变四件事：

|      | 输出预算 | 思考                     | 候选标注 | 首次 + 纠错超时 |
| ---- | -------- | ------------------------ | -------- | --------------- |
| 简单 | 64       | 关闭（gpt-oss 取最低档） | 无       | 20s + 10s       |
| 中等 | 128      | 按模型能力               | 无       | 45s + 15s       |
| 困难 | 256      | 按模型能力               | 有       | 45s + 15s       |

三档的「首次 + 纠错」预算都不超过 60 秒；中等档的首次超时与引入难度之前一致（45 秒），只缩短了纠错重试，因此本来刚好够用的慢模型不会变得不够用。困难档的棋力主要来自**候选标注**而不是更长的思考：标注只增加输入 token，几乎不影响响应时间。标注内容（被吃子的公开称谓与子力价值、这一步是否会翻开己方暗子）全部由公开局面推导，不接触未翻棋子的真实身份。

每一档的策略文本（规则 + 行棋思路）放在 `server/ai/strategies/{easy,medium,hard}.md`。放一份同名文件到 `.local/strategies/` 即可覆盖对应档位而不改代码；文件缺失、空白、不可读、不是普通文件（如命名管道）、超过 8 KiB 或是软链接时一律回落到内置文本，坏掉的策略文件不会让对局起不来。策略文本只在服务端与 Runner 进程内使用，不下发浏览器。

不同模型、不同硬件下三档的实际强弱差距需要实测，本项目不作与硬件无关的棋力承诺。

控制台分别展示模型实际返回的 thinking、模型实际返回的 final、最终着法、简短理由、模型阶段耗时和应用侧提交耗时。显示及日志中的模型文本会移除 ANSI、OSC 和危险终端控制字符，界面明确标为“模型原始文本（已移除终端控制字符）”。能力接口不可用时，能力显示为未知，不会伪造 thinking。

模型速度取决于模型体积、量化、上下文和本机硬件，本项目不作硬件无关的速度承诺。

### API 与数据边界

完整契约见 [docs/API.md](docs/API.md)。对既有对局的状态写入必须携带 `expectedRevision`；并发失败统一为 `409`。重新邀请还必须匹配当前房间码，防止延迟重试撤销后来入座的设备。请求体只接受 UTF-8 JSON，最大 16 KiB，所有 Schema 拒绝未知字段。

服务默认只绑定回环地址，并以规范化后的 socket 对端地址识别真实调用方；`Host`、`Forwarded`、`X-Forwarded-For` 和 `X-Real-IP` 都不能证明请求来自本机。loopback Host 只接受 loopback 对端。开启局域网模式后改为监听 `0.0.0.0`，LAN Host 必须精确匹配服务器当前真实网卡的私网 IP 字面量，socket 对端也必须来自 loopback 或私网；域名、其他私网 Host、公网对端和同 IP 异端口 Origin 均被拒绝。

`POST /games`、`POST /rooms`、网络开关、AI 与 Agent 会话**始终仅限真实 loopback**；非 LAN 对局的落子、认输和悔棋同样只限本机。远程设备只可兑换房间码并使用座位令牌对弈。`agent-session` 会在宿主机上拉起终端进程，因此绝不对 LAN 开放。

局域网对战没有账号体系：同一网段内拿到房间码的人就能入座，且传输是明文 HTTP。新座位会立即保存在当前页面内存中；localStorage 仅用于尽力恢复刷新后的座位，并以单调签发代次、独立存储槽和保留代次的凭据墓碑协调多标签页；条件清理不会删除可能刚被旧版标签页替换的共享指针。即使浏览器拒绝写入也不影响当前对局；若旧凭据已持久化而安全清理失败，换局会停止并提示释放站点存储空间。请只在可信的家庭网络下开启，详见 [SECURITY.md](SECURITY.md)。

对局只保存在内存中，重启服务后丢失；浏览器的“恢复上局”仅保存本机 game ID，不持久化棋局。

Agent 凭据文件位于 `.local/agent-sessions/`、权限为 0600，并在停止、终局、退出、重启或过期时删除。JSONL 日志位于 `.local/agent-logs/`，可能包含公开棋盘、合法着法、模型 thinking/final、理由、错误和耗时；单文件上限 5 MiB，只保留一个轮转文件，启动时清理 7 天前日志。请按本地敏感日志管理。

### 验证与覆盖范围

```bash
npm run lint
npm run format:check
npm run typecheck
npm run test:coverage
npm run test:security
npm run test:stress
npm run test:e2e
npm run build
npm run release:check
```

在装有兼容模型的 macOS 真机上，可额外运行真实 Ollama 双路径烟测：

```bash
npm run smoke:ollama
```

默认使用 `qwen3.6-uncensored:q3kp` 验证实际 thinking 流，以及 `qwen3-vl:4b` 验证 `think=false` 的 direct 流；可分别用 `OLLAMA_THINKING_MODEL`、`OLLAMA_DIRECT_MODEL` 覆盖。

Playwright 覆盖 Chromium、Firefox 和 WebKit，并含固定桌面/平板/手机横竖屏矩阵与 axe 检查。重型 10 分钟压力测试在手动 workflow 和发布标签上运行；普通本地压力门禁使用相同断言的短时模式。真实 Ollama、Safari、VoiceOver、iTerm2 和 Terminal 仍属于 macOS 手动验收。

Windows/Linux CI 会真实启动 Node 服务并测试终端命令与带空格路径的参数构造，但不会声称验证了每个桌面终端 GUI。自动压力使用假 Ollama/假终端，避免把硬件和模型波动当成产品回归。

### 模块职责

- `engine/`：布子、走法、终局、公开投影和有界内存存储
- `server/lan/`、`server/net/`、`server/network.ts`：局域网房间与座位、Host 策略、监听地址控制
- `server/`：REST API、Ollama、Agent Runner、终端与会话生命周期
- `shared/`：前后端共享契约
- `src/`：React 界面、可访问棋盘和恢复流程
- `skills/`：只通过公开 API 操作棋局的 Agent Skill
- `tests/`：单元、集成、安全、压力与浏览器测试
