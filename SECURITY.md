# Security Policy

## Supported versions

安全修复目前只面向最新的 `1.x` 版本。

## Reporting a vulnerability

请优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告功能。不要在公开 Issue 中披露 token、会话文件、日志、未公开漏洞细节或可直接利用的 PoC。维护者会确认报告、评估影响，并在修复可用后协调披露。

## Threat model

覆子默认是单用户、本机回环应用，不提供账号体系或公网部署保证。安全假设包括：

- **默认只监听 `127.0.0.1`**；局域网监听必须由本机用户显式开启（`--lan` / `LAN=1`，或首页开关）；
- Ollama 与 Agent Runner **始终**只通过 loopback HTTP 使用，与监听模式无关；
- 本机用户能够读取自己有权限访问的进程和文件；
- `.local/agent-sessions` 是短期凭据，不应同步或提交；
- Agent 日志包含公开棋局和模型文本，应按敏感本地日志处理；
- 指定 Seed 的创建者天然知道 Seed，隔离保证是 API 不额外泄漏活动 Seed。

### 局域网对战的信任模型

开启局域网模式后，服务改为监听 `0.0.0.0`，安全边界随之变化，请在开启前确认你接受以下几点：

- **同一网段内拿到房间码的人就能入座。** 房间码是 6 位、来自 32 个无歧义字符（约 10⁹ 种），并对猜错的兑换做了限流，但它是一次性邀请凭据，不是身份认证。
- **适用于家庭 Wi-Fi，不适用于咖啡厅、宿舍、公司等不可信网络。**
- **传输是明文 HTTP。** 同一 Wi-Fi 下的嗅探者可以看到座位令牌与完整的公开局面。局域网自签证书的体验代价过高，因此本项目不做 TLS——局域网模式假定网络本身可信。
- **本机身份来自 socket 对端，不来自请求头。** 服务规范化 IPv4、IPv4-mapped IPv6 与 IPv6 loopback；`Host`、`Forwarded`、`X-Forwarded-For` 和 `X-Real-IP` 都不参与来源授权。
- loopback Host 只接受 loopback 对端；LAN Host 必须精确匹配服务器当前真实网卡的**私网 IP 字面量**，socket 对端也必须是 loopback 或私网地址。域名、其他私网 Host 与公网对端均拒绝。这使 DNS rebinding 与公网端口转发无法扩大支持边界，代价是 mDNS 名称（`xxx.local`）也会被拒。CGNAT / Tailscale（100.64/10）**有意排除**。
- LAN 浏览器 Origin 必须与请求 Host 的 IP 和端口同时一致；同一台机器上其他端口提供的页面也不能跨域读取座位签发响应。只有 loopback 下的 Vite 开发页面保留跨端口例外。
- **座位令牌先保存在当前 React state/ref 中**；同源 localStorage 只是尽力而为的刷新恢复，浏览器拒绝存储不会使刚签发的座位失效，纯内存座位终局后也可正常换局。普通导航保留未加载的恢复席位；确认离开或开始另一局时会先以本席位认输并结束旧房间，成功后才清理/替换令牌。每次签发使用单调代次与独立存储槽，仍打开的旧版标签页后来签发的共享记录会提升为下一代；清理只写入针对该凭据且保留逻辑代次的墓碑，不删除缺少原子比较保障的共享指针。较新墓碑同时屏蔽所有更旧槽，因此时钟回拨或旧标签页都不能删除新凭据，结束新版座位也不会复活旧凭据。若已持久化凭据的墓碑写入失败，换局会中止并保留恢复入口。瞬时读取或结束失败同样保留恢复入口，只有明确不存在的对局才清理。
- **`POST /games`、`POST /rooms`、网络开关、AI 与 Agent 会话始终仅限真实 loopback**；非 LAN 对局的落子、认输与悔棋也只限 loopback。远程设备只保留兑换房间码和持座位令牌对弈的能力；`agent-session` 会在宿主机上拉起终端进程，绝不对局域网开放。
- 监听关闭、重绑和失败回滚进入单一 FIFO；状态接口以 `pending`、`targetMode` 与 `listening` 报告真实 listener，不把目标模式冒充为已生效模式。关机关闭是永久终态，响应完成回调中的晚到切换不能重新绑定端口。

LAN 写操作按座位令牌鉴权（`Authorization: Bearer`），读操作保持开放。带令牌读取会额外投影该凭证的 `valid` / `revoked` / `invalid` 状态，因此替补占据同一颜色后，旧设备仍能明确发现自己已被移出，也不会继续获得该颜色的「你」视角；这项元数据不包含令牌或隐藏棋子信息。两个座位收到**完全相同的棋盘公开投影**。

重新邀请同时要求当前 `expectedRevision` 与 `expectedRoomCode`。旧请求的延迟重放只能得到冲突，不能再次旋转已经分享的新码或撤销后来入座的替补。

### 经同意的信息披露

局域网悔棋需要对方同意。批准悔棋会**永久泄漏**那一步所揭示的暗子身份：服务端会正确地把棋子重新盖回，但对手在上一轮轮询里已经看到了。这正是采用「需对方同意」而非单方悔棋的原因——对手是在知情后自愿交换这个信息的。界面在审批时会明确提示这一点。

每次悔棋请求另有 128-bit 随机 ID。处理回应时必须同时匹配 `requestId` 与 `expectedRevision`；因此即使同一 revision 上先后出现两个请求，旧设备延迟送达的批准也不能处理后一个请求。

若要跨用户或跨主机（而非同一可信局域网）部署，必须另行增加认证、授权、TLS、持久化隔离和运维审计；这不在支持范围内。
