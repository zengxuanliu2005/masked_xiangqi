# 图文交付检查记录

检查日期：2026-09-09。

## 成品

- README：特色叙事、五张横版图、使用与技术文档入口、简短许可说明。
- 使用指南：安装启动、三种对战方式、规则、恢复与常见问题。
- Changelog：保留既有版本历史，在末尾单独收录从 README 迁入的实现说明。
- 小红书：作者第一人称正文、主标题与两个备选标题、六张 1080 × 1440 PNG。
- 图片背景由内置 imagegen 生成；界面来自 Computer Use 的 Safari 实拍，文字和截图通过独立 HTML/CSS 排版。
- 可编辑源文件、提示词、原始截图均保存在 `res/`；参考文件 `res/example/` 保留原样，并从 Prettier 格式化范围排除。

## 内容与视觉

- 揭面前后是同一实际对局的同一手：红方按兵移动，揭晓为黑方车。
- 本地 `qwen3-vl:4b` 已实际完成第一手，未伪造模型输出或棋力结果。
- 局域网图展示真实模式入口及空白加入框，不包含有效邀请码；没有宣称本轮进行了跨设备实测。
- 逐张检查 11 张图片，修正副标题与截图距离、底部小字对比度；所有标题与截图区域均无碰撞、无内容越界。
- GitHub Markdown API 渲染 README，再以接近 GitHub 的本地样式检查 1280 与 390 像素宽度。两种宽度均无横向溢出，五张图全部加载。该预览不等同于 GitHub 网站完整外壳。
- Markdown 本地目标文件均存在；Git diff 空白检查通过。

## 仓库检查

`npm run check` 通过：lint、Prettier、TypeScript、24 个测试文件的 302 项测试、覆盖率门槛和生产构建。

| 覆盖率     | 结果   |
| ---------- | ------ |
| Statements | 87%    |
| Branches   | 80.88% |
| Functions  | 91.23% |
| Lines      | 89.25% |

维护表已核对：没有修改 `engine/`、`server/`、`src/`、`shared/`、npm 脚本、接口、错误码、安全边界、测试布局或覆盖率门槛，因此 `AGENTS.md` 与 `CLAUDE.md` 无需更新。

本次交付提供小红书待发布素材，未代为发布。`output/` 中的检查预览为临时产物，不纳入交付源码；用户提供的 `res/example/` 参考文件保留在本地，不随本次提交上传。
